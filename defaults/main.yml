---
virtualbox_version: "5.0"
virtualbox_rpm_package: "VirtualBox-{{ virtualbox_version}}"
epel_release: "epel-release-7-5.noarch.rpm"
epel_path: "/tmp/{{ epel_release }}"
vb_repo_url: "http://download.virtualbox.org/virtualbox/rpm/rhel/virtualbox.repo"
epel_repo_url: "http://dl.fedoraproject.org/pub/epel/7/x86_64/e/{{ epel_release }}"
yum_repo_path: "/etc/yum.repos.d/"
